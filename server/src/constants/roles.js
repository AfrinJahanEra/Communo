/** Platform-level roles (server-scoped roles come in Phase 2). */
export const GLOBAL_ROLES = Object.freeze({
  USER: "user",
  ADMIN: "admin",
});

export const GLOBAL_ROLE_VALUES = Object.values(GLOBAL_ROLES);
